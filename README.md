# discovery-hub (Core MVP)

This repo generates a simple hub page from `data/daily.csv` and publishes it via GitHub Pages.

Outputs in `docs/`:
- `all.html`
- `sitemap.xml`
- `rss.xml`
- `robots.txt`
- `index.html`
